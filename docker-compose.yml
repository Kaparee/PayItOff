version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: payitoff_postgres
    restart: always
    environment:
      POSTGRES_USER: payitoff_admin
      POSTGRES_PASSWORD: SuperSecretPassword123!
      POSTGRES_DB: PayItOff
    ports:
      - "5442:5432"
    volumes:
      - payitoff_data:/var/lib/postgresql/data

volumes:
  payitoff_data: